org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# All current plugins use AndroidX. Legacy Jetifier cannot transform the
# Java 24 multi-release classes in the plugin test dependency byte-buddy.
android.enableJetifier=false
