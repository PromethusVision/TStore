// Copy to ReleaseSigning.xcconfig only in a secure local/CI workspace.
// The populated file is gitignored and optional for unsigned compile checks.
DEVELOPMENT_TEAM = REPLACE_WITH_APPLE_TEAM_ID
PROVISIONING_PROFILE_SPECIFIER = REPLACE_WITH_DISTRIBUTION_PROFILE_NAME
CODE_SIGN_IDENTITY = Apple Distribution
